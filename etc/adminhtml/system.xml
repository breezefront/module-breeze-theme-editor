<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="urn:magento:module:Magento_Config:etc/system_file.xsd">
    <system>
        <section id="breeze_theme_editor" translate="label" type="text" sortOrder="321" showInDefault="1" showInWebsite="1" showInStore="1">
            <class>separator-top swissup-breeze-theme-editor-config-item</class>
            <label>Breeze Theme Editor</label>
            <tab>swissup</tab>
            <resource>Swissup_BreezeThemeEditor::config</resource>

            <group id="general" translate="label" type="text" sortOrder="10" showInDefault="1" showInWebsite="1" showInStore="1">
                <label>General Settings</label>
                <field id="enabled" translate="label comment" type="select" sortOrder="10" showInDefault="1" showInWebsite="1" showInStore="1">
                    <label>Enable Theme Editor</label>
                    <source_model>Magento\Config\Model\Config\Source\Yesno</source_model>
                    <comment>Enable or disable Breeze Theme Editor mode.</comment>
                </field>
                <field id="auth_header" translate="label comment" type="text" sortOrder="20" showInDefault="1" showInWebsite="0" showInStore="0" canRestore="1">
                    <label>GraphQL Authorization Header</label>
                    <comment><![CDATA[HTTP header used to pass the admin Bearer token to the GraphQL endpoint. Default: <b>Authorization</b>.<br>
                        If the site is protected by HTTP Basic Auth (.htaccess), Apache/nginx intercepts the standard <b>Authorization</b> header,
                        returns 401 and the browser shows an endless password prompt. Set a custom header (e.g. <b>X-Bte-Authorization</b>) to work around it —
                        the module copies its value into <b>Authorization</b> server-side, before Magento validates the token.<br>
                        Allowed characters: letters, digits and hyphen.]]></comment>
                </field>
            </group>
        </section>
    </system>
</config>
